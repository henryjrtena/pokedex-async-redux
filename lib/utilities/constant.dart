import 'package:flutter/material.dart';

const offSet = '0';
const limit = '151';

const appName = 'P O K E D E X';
const homePageLabel = 'Home Page';

const primaryColor = Color.fromRGBO(239, 83, 80, 1);

const imageUri = 'https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/';

const overviewPagePadding = EdgeInsets.all(10.0);
const pokemonOverviewPerColumn = 2;
